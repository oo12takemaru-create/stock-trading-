# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
.venv

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# バックテスト出力
*_result.csv
*_chart.png
*.json
!signal.json
!prices.json
# 無料版スキャナーの公開データ(GitHub Pages配信用)はコミット対象
!docs/free_scanner.json
# 遅延なしの地合い専用JSON(指示書§8-2・jiai.htmlが優先取得する)
!docs/market_jiai.json
# ジンクス(アノマリー)50本の検証結果。MCP の get_anomaly_summary が読む
!docs/anomaly_results.json
# 酒田五法12本の検証結果。MCP の get_candlestick_verdict が読む
# (mcp/tools/build_candlestick_verdict.py が生成・verify_* が元CSVと照合)
!docs/candlestick_verdict.json
# 出来事27種のあとの動き。MCP の get_event_reaction が読む
# (mcp/tools/build_event_reaction.py が生成・verify_* が元CSVと照合)
!docs/event_reaction.json
# テクニカル指標26種の検証結果。MCP の get_indicator_verdict が読む
# (mcp/tools/build_indicator_verdict.py が生成・verify_* が元CSVと書籍の原稿と照合)
!docs/indicator_verdict.json
# レバレッジ・インバースETFの減価。MCP の get_etf_decay が読む
# (mcp/tools/build_etf_decay.py が生成・verify_* が元CSVと元mdと照合)
!docs/etf_decay.json
# 🔒 trades.json / stops.json は非公開リポジトリ(stock-trading-private)管理。
#    公開リポジトリには絶対コミットしない(例外を作らないこと)
cost_tax_analysis_result.csv
stop_loss_simulator_result.csv
sector_threshold_analysis.csv

# トレード実行ログ(機密)
trade_history.csv
positions.csv

# 一時ファイル
*.tmp
*.log
tmp/
signal.md
signal_final.md
signal.json

# 環境変数
.env
*.env

# OS
Thumbs.db

.vercel

# MCP サーバー(mcp/)の設定JSONはコミット対象
!mcp/package.json
!mcp/vercel.json
mcp/node_modules/
mcp/.wrangler/

# レジストリ登録用の JSON はコミット対象。秘密鍵は絶対に入れない
!mcp/registry/server.github.json
!mcp/registry/server.dns.json
*.pem
mcp/registry/server.json

# 公開する成果物は *.json の除外から戻す（GitHub Pages で配るため）。
# ★ここに書き忘れると、ワークフローが「変更なし」で静かに素通りする。
#   2026-09-06 に precompute/thresholds.json で同じ罠を踏んだ。
!docs/portfolio_stats.json

# 自社株買い: TDnetの生ログ保全（TDnetは27営業日で消えるので、消える前に残す）
!buyback_raw/
!buyback_raw/*.json
!docs/buyback.json
!docs/buyback_history.json
!tests/classify_cases.json
!docs/karauri_score.json
!docs/cot_score.json
!karauri_events/
!karauri_events/*.json

# score3_lite の中間データ（週次キャッシュ・計算結果CSV）はコミットしない
precompute/cache/
# 投機筋の10年の物差し（週次）
!docs/cot_scale.json
# トップの要約1本
!docs/board.json
